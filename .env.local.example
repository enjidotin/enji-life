# Populated by `npx convex dev` on first run
NEXT_PUBLIC_CONVEX_URL=

# Required by Convex Auth — set on the deployment with:
#   npx convex env set JWT_PRIVATE_KEY "$(npx @convex-dev/auth --jwt-private-key)"
#   npx convex env set JWKS "$(npx @convex-dev/auth --jwks)"
#   npx convex env set SITE_URL http://localhost:3000
